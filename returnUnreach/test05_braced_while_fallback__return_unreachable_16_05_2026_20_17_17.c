/* test05 - return inside braced while-body, no initialized vars before return.
 * needsBraces = false, expressionFromScope = false.
 * Expected: fallback literal inserted after "return n".                */
int loop_return(int n) {
    while (n > 0) {
        return n;
    }
    return 0;
    (2 - 2) * 10;
}
