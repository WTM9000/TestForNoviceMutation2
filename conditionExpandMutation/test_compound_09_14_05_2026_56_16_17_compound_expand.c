#include <stdio.h>

int main() {
    int a = 5;
    int b = 3;
    if (a > 0) {
            if (b > 0) {
                printf("line one\n");
                printf("line two\n");
            }
    }
    return 0;
}
